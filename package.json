{
  "name": "soundback",
  "version": "0.0.1",
  "description": "backend for soundcloud mashup",
  "main": "server.js",
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "test": "mocha --timeout 3000 --reporter nyan test",
    "start": "nodemon start_server.js",
    "dev": "nodemon start_server.js dev",
    "coverage-serve": "istanbul --include-all-sources cover _mocha -- -R spec; static coverage/lcov-report -a 0.0.0.0",
    "coverage": "istanbul --include-all-sources cover _mocha -- -R spec",
    "codacy": "mocha --timeout 3000 --reporter mocha-lcov-reporter test",
    "test-watch": "mocha --timeout 3000 --reporter nyan test --watch --colors",
    "docs": "apidoc -o apidoc/ "
  },
  "author": "paszin",
  "license": "MIT",
  "dependencies": {
    "async": "^1.5.2",
    "hapi": "^13.0.0",
    "joi": "^8.0.4",
    "lodash": "^4.6.1",
    "mongoose": "^4.4.2",
    "mongoose-sequence": "^1.0.0",
    "node-soundcloud": "0.0.5",
    "request": "^2.69.0"
  },
  "devDependencies": {
    "apidoc": "^0.15.1",
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "mocha-istanbul": "^0.2.0",
    "codacy-coverage": "^1.1.3",
    "mocha-lcov-reporter": "^1.2.0"
  }
}
